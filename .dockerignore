# Dependências e build local
node_modules
.next
out
build
coverage

# Git / IDE
.git
.gitignore
.gitattributes
.vscode
.idea
*.md
!README.md

# Env e secrets (NEXT_PUBLIC_* entram como build args)
.env
.env.*
!.env.example

# Vercel / CI local
.vercel
.github

# Docs e artefatos que não entram na imagem
docs
agent-transcripts
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker (não precisa copiar a si mesmo no contexto além do Dockerfile)
docker-compose*.yml
Dockerfile*
